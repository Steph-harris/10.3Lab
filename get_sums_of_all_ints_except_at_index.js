var integers = process.argv

//take in 4 numbers
//take 3 of 4 and add them (not the one at the index)
//run loop, making sure that the same 3 aren't being repeated
//return the values in an array
// make a function that keeps returned valus from being equal 
// for loop(at this position[i] skip this number and add the rest together)

for(i=2; i<integers.length; i++){
  var parsedArg = parseFloat(integers[i]);
  console.log(skipExtraArgs);
}